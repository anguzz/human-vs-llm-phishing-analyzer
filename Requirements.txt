# Tested with Python 3.8-3.12. Any combination at or above these versions works;
# accuracy numbers are identical across them, coefficients differ by < 0.0001.
pandas>=2.0
scikit-learn>=1.3
numpy>=1.24
scipy>=1.10